let logginCredentials = {
    username : "impu1s3e",
    password : "password",
    age : 20
}

// console.table(logginCredentials)
// console.log(logginCredentials)
// console.log(`The username is ${logginCredentials.username} \nAnd the password is ${logginCredentials.password}`)

logginCredentials.name = "wahid"

// console.table(logginCredentials)

//Another way to access objects is 

// console.log(logginCredentials["username"]) //advantage for this is we can access objects which have spaces in it

//declaring an array below

let heroes = ['a', 'b', 'c', true, false]

console.log(heroes.at(0));
console.log(heroes.at(4));
console.log("1" + 1)
console.log(1 + "1")



